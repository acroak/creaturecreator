<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <%_include ../views/partials/head.ejs %>
    </head>
    <body>

<%_include ../views/partials/nav.ejs %>

        <div class="row gallery-container">
          <div class="row">
              <% for(let i = 0; i < beasts.length; i++){ %>
                <div class="gallery-div col-sm-4">
                  <a href="/beasts/<%=beasts[i].id; %>">
                    <img src="<%= beasts[i].beastImg %>" alt="<%= beasts[i].name %>">
                  </a>
                  <div class="">
                    <%= beasts[i].name %>
                    <br>
                    <a href="/users/<%= beasts[i].artist %>/gallery"><%= beasts[i].artist %></a>
                  </div>
                </div>

              <% } %>
          </div>
        </div>

        <%_include ../views/partials/footer.ejs %>


    </body>
</html>
